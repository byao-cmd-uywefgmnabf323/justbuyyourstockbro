// lib/config.ts
export const config = {
  // Example: API endpoints, keys, etc. Use process.env for secrets
  SUGGEST_API: process.env.NEXT_PUBLIC_SUGGEST_API || "/api/screener/suggest",
  // Add more config as needed
};
